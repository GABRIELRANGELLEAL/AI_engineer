cd /mnt/c/Users/Leal/AI_Projects/analytics_content_agent

docker build -f Docker/Dockerfile -t analytics-content-agent:ubuntu .

docker run --rm -it \
  --env-file .env \
  -p 8000:8000 \
  -v "$PWD/outputs:/app/outputs" \
  -v "$PWD/runs:/app/runs" \
  -v "$PWD/data:/app/data" \
  analytics-content-agent:ubuntu

terminal back
cd c:\Users\Leal\AI_Projects\analytics_content_agent
python -m uvicorn app:app --port 8000 --reload

terminal front
cd c:\Users\Leal\AI_Projects\analytics_content_agent\frontend
npm run dev

